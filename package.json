{
    "name": "@flowcircuits/wasmles",
    "version": "1.0.1",
    "license": "MIT",
    "author": "FLOW CIRCUITS LLC",
    "repository": "https://github.com/flowcircuits/wasmles",
    "scripts": {
        "build": "wasm-pack build --target web --out-dir pkg/browser && wasm-pack build --target nodejs --out-dir pkg/node",
        "postbuild": "node postbuild.js",
        "test": "cargo test -- --nocapture"
    },
    "devDependencies": {
        "wasm-pack": "^0.12.0"
    }
}
