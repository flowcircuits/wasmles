import { LinearEquation } from "./index";

export function solveLinearEquations(
    equations_value: LinearEquation[]
): Map<string, number>;

export function solveLinearEquationClusters(
    clusters_value: LinearEquation[][]
): Map<string, number>;
